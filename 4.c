#include <stdio.h>
int main(void) {
    int x = 5;
    printf("Initial value: %d\n", x);

    x++; // increment
    printf("After increment: %d\n", x);

    x--; // decrement
    printf("After decrement: %d\n", x);

    return 0;
}
